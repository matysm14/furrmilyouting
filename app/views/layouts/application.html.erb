<!DOCTYPE html>
<html>
  <head>
    <title>Furrmilyouting</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/5878c30bca.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="d-flex flex-column vh-100" style="display: flex; flex-direction: column;">
    <header class= "text-dark pt-1 pb-1" style="background-color: #f8f2e5;">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container px-3">
          <a class="navbar-brand p-3" href="/"><%= image_tag('Logo.png') %></a>

          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation" >
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

              <% if user_signed_in? %>
                <li class="nav-item">
                 <%= link_to user_path(current_user), class: 'nav-link' do  %>
                 <i class="fa-solid fa-user"></i> マイページ
                 <% end %>
                </li>

                <li class="nav-item">
                 <%= link_to destroy_user_session_path, method: :delete, class: 'nav-link' do  %>
                 <i class="fa-solid fa-right-from-bracket"></i> ログアウト
                 <% end %>
                </li>

              <% else %>

              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <div>
      <%= yield %>
    </div>

    <footer class="text-dark pt-4 pb-4" style="margin-top: auto; background-color: #f8f2e5;">
      <div class="container mt-0">
        <div class="row">

          <div class="col-md-8">
            <% if user_signed_in? %>
              <h5>新規登録</h5>
              <p>登録されていない飲食店や宿泊施設は登録申請ができます。</p>
              <%= link_to "飲食店の新規登録申請", new_eatery_path %></br>
              <%= link_to "宿泊施設の新規登録申請", new_lodging_path %>
            <% end %>
          </div>

          <div class="col-md-3 offset-md-1">
          </div>

        </div>
      </div>
    </footer>

  </body>
</html>